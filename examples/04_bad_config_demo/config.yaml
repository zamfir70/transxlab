## BAD CONFIG — intentional mistakes for TransXLab to catch
## This mirrors the AC-v2 postmortem: $665 wasted on a doomed run
base_model: "meta-llama/Llama-3-8B"
method: full
lr: 1e-4
epochs: 10
per_device_train_batch_size: 32
gradient_accumulation_steps: 1
weight_decay: 0.0
output_dir: "./output/bad_run"
data_dir: "./data"
train_file: "train.jsonl"
input_field: "input"
output_field: "output"
# Note: no diversity_loss_weight set — should trigger warning for creative task
# Note: lr=1e-4 is too high for full fine-tuning — should trigger warning
# Note: 10 epochs on fine-tuning — should trigger warning
# Note: full fine-tune of 8B model on 24GB VRAM — should trigger VRAM warning
